import React from 'react';

const UserProfile = () => {
  return (
    <div>
      <h2>User Profile</h2>
      {/* TODO: Display and edit user info, badges, etc. */}
    </div>
  );
};

export default UserProfile;
